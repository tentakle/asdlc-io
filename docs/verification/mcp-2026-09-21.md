# MCP preview verification — 2026-09-21

This record supports the [transport contract](../../specs/mcp-transport/spec.md)
and [setup instructions](../../README.md#model-context-protocol-mcp). It is preview
evidence, not production-cutover approval.

- Pull request: [draft #82](https://github.com/villetakanen/asdlc-io/pull/82).
- Preview: <https://deploy-preview-82--asdlc-io.netlify.app/mcp>.
- Netlify deployment: `6ab0fd1125c49c0008b0ac76`, deploy-preview context.
- Tested server revision: `7d03679` (`feat(mcp): implement modern-only stateless transport (AL-50)`).
- Server identity: `asdlc-knowledge-base` / `0.25.0`.
- Wire checks completed at `2026-09-21T09:48:08.466Z`.
- Reference-client checks completed at `2026-09-21T09:48:04.313Z`.

## Results

| Check | Evidence |
| --- | --- |
| Actual client | `@modelcontextprotocol/client@2.0.0`, pinned negotiation, MCP `2026-07-28` |
| Discovery | Successful SDK connection and explicit discovery; only modern revision and tools advertised |
| Tool catalog | Exactly `list_articles`, `get_article`, `search_knowledge_base` |
| Retrieval | SDK listed articles, searched “context engineering”, then retrieved returned slug `context-engineering` (11,891 characters) |
| Wire identity | Every captured SDK exchange carried `2026-07-28` in both header and body metadata and received HTTP 200 |
| Statelessness | Wire script called tools/list before discovery; correlated IDs and no session header |
| Legacy boundary | All published 2024/2025 revisions and legacy initialization rejected; GET/DELETE return 405; notifications discarded with empty 202 |
| Browser access | Exact production and configured preview origins pass preflight and POST; unrelated/null origins fail without grants |
| Caching | Five-minute public MCP catalog hints; HTTP no-store; no catalog hints on tool results |

The wire script verifies rejection responses; local dispatch-spy tests establish
that rejected requests do not execute retrieval. No server telemetry claim is made.

## Client coverage gap

Claude Code `2.1.278` is **unverified**. Its local auth check reported signed out;
no Claude session, runtime negotiation, or tool execution was tested. Ville
explicitly requested “Record Claude as unverified” on 2026-09-21. No other editor
or agent host is certified by this reference-client result.

The accepted ADR's Claude host check remains outstanding. Keep the PR in draft
and resolve that gate before production cutover. This record does not authorize
production release or legacy fallback.

## Reproduction

```bash
pnpm test:mcp-preview https://deploy-preview-82--asdlc-io.netlify.app 7d03679
pnpm test:mcp-client https://deploy-preview-82--asdlc-io.netlify.app 7d03679
```

The commit argument records the independently checked deployment revision; the
scripts do not infer it from server version. Preview aliases can move: verify
the current Netlify deployment before rerunning. Set `MCP_PREVIEW_ORIGIN` to this
preview's exact origin with Functions scope and deploy-preview context.
